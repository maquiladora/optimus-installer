user = $MAILSERVER_MARIADB_USER
password = $MAILSERVER_MARIADB_PASSWORD
dbname = mailserver
hosts = 127.0.0.1
query = SELECT CONCAT('/srv/mailboxes/',email) AS maildir FROM mailboxes WHERE email='%s' AND status=1
